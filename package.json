{
  "name": "dsh-plugins",
  "private": true,
  "description": "DSH host 层插件集：vision-router（识图降级）/ sandbox-extra-roots（沙盒额外目录）/ adaptive-perf（极简性能自适应）",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/winliyou/dsh-plugins.git"
  },
  "keywords": [
    "dsh",
    "deepseek-harness",
    "plugin",
    "cordis"
  ],
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "bun@1.3.14",
  "devDependencies": {
    "@deepseek-ai/dsh-typert-protocol": "^0.1.0-rc.7"
  },
  "scripts": {
    "test": "bun scripts/test.mjs",
    "test:ci": "bun scripts/test.mjs",
    "build": "echo 'no build step: host plugins are plain ESM, client bundles are hand-written CommonJS'"
  }
}
